package window

/* type Window struct {
	Base   screen.Window
	Buffer screen.Buffer
}

func CreateWindow(s screen.Screen, title string, width int, height int) screen.Window {
	w, err := s.NewWindow(&screen.NewWindowOptions{
		Title:  title,
		Width:  width,
		Height: height,
	})
	if err != nil {
		panic(err)
	}
	return w
} */
